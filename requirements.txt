# AI Backend Dependencies
# Core dependencies for PDF processing, OCR, and text parsing

# FastAPI and web framework dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Google Cloud Vision API for OCR
google-cloud-vision>=3.4.0
google-auth>=2.23.0
google-api-core>=2.12.0

# PDF processing and image manipulation
PyMuPDF>=1.23.0  # For PDF to image conversion
Pillow>=10.0.0   # For image processing
pdf2image>=1.16.3  # Alternative PDF converter

# Environment configuration
python-dotenv>=1.0.0

# File and data handling
pathlib2>=2.3.7  # Enhanced path handling (Python 3.4+ compatibility)

# Optional: Development and testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
coverage>=7.3.0
black>=23.9.0
flake8>=6.1.0
mypy>=1.6.0

# Optional: Performance and monitoring
redis>=5.0.0  # For caching
aiohttp>=3.9.0  # For async HTTP requests
psutil>=5.9.0  # For system monitoring

# Optional: Database support (for future features)
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.29.0  # PostgreSQL async driver

# Optional: Message queue support
celery>=5.3.0
kombu>=5.3.0

# Optional: Cloud storage
google-cloud-storage>=2.10.0
boto3>=1.29.0  # AWS S3 support

# Optional: Metrics and monitoring
prometheus-client>=0.18.0
structlog>=23.2.0

# Security
cryptography>=41.0.0
passlib[bcrypt]>=1.7.4

# Optional: Additional OCR engines
pytesseract>=0.3.10  # Tesseract OCR as fallback
easyocr>=1.7.0       # EasyOCR as alternative