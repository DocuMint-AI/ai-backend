# AI Backend Environment Configuration
# Copy this file to .env and update with your actual values

# Google Cloud Vision API Configuration
GOOGLE_CLOUD_PROJECT_ID=ai-backend-vision-sa 
GOOGLE_CLOUD_CREDENTIALS_PATH=data/.cheetah/gcloud/vision-credentials.json
# Credentials are not included in the repository for security reasons.
# Please set the path to your service account credentials JSON file.

# OCR Configuration
LANGUAGE_HINTS=en,es,fr
IMAGE_FORMAT=PNG
IMAGE_DPI=300

# Data Storage Configuration
DATA_ROOT=/data
MAX_FILE_SIZE_MB=50

# FastAPI Configuration
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Optional: Additional Google Cloud settings
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json  # Alternative credential method
# GOOGLE_CLOUD_REGION=us-central1

# Optional: Database configuration (for future use)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Optional: Redis configuration (for future caching)
# REDIS_URL=redis://localhost:6379/0

# Security (for production)
# SECRET_KEY=your-secret-key-here
# ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com